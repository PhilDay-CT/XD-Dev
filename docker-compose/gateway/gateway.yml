#
# Docker Compose file for a software version
# of a Gateway.
#

name: "ctxd"

include:
  - ./config.yml
  
services:

  #
  # Gateway
  #
  import_diode:
    image:  ghcr.io/configuredthings/ctxd_diode:${ctxd_version}
    hostname: import.gateway.ctxd
    networks:
      ctxd_low:
        aliases:
          - gateway.low.ctxd
      ctxd_high:
    configs:
      - source: import_diode
        target: /opt/ct/config/diode.sfjs
      - source: status_high
        target: /opt/ct/system/config/status.sfjs

          
  export_diode:
    image:  ghcr.io/configuredthings/ctxd_diode:${ctxd_version}
    hostname: export.gateway.ctxd
    networks:
      ctxd_high:
        aliases:
          - gateway.high.ctxd
      ctxd_low:     
    configs:
      - source: export_diode
        target: /opt/ct/config/diode.sfjs
      - source: status_high
        target: /opt/ct/system/config/status.sfjs
     
  
networks:
  ctxd_low:
    name: ctxd_low
  ctxd_high:
    name: ctxd_high
  



